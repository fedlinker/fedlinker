const chalk = require('chalk');

exports.command = 'compile [options]';
exports.describe = 'Compile React components library to node module';

exports.handler = argv => {
  console.log(chalk.green('Work in progress...'));
};
