const chalk = require('chalk');

exports.command = 'build [options]';
exports.describe = 'Build React components library to static document';

exports.handler = argv => {
  console.log(chalk.green('Work in progress...'));
};
